# Database
DATABASE_URL="mysql://root:root@localhost:3308/coffee_house"

# Server
PORT=3000
NODE_ENV=development

# Security - generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
APP_SECRET=change-me-to-a-real-secret
